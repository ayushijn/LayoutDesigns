<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layoutdesigns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.DesignOneActivity" android:theme="@style/NoActionTheme"/>
        <activity android:name=".Activities.DesignTwoActivity" android:theme="@style/NoActionTheme"/>
        <activity android:name=".Activities.DesignThreeActivity" android:theme="@style/NoActionTheme"/>
        <activity android:name=".Activities.SignUpActivity" android:theme="@style/NoActionTheme"/>
    </application>

</manifest>